# Py2ONTO Visual Editor — Python dependencies (pinned versions)
#
# Install:  pip install -r requirements.txt
#
# Note: vis-network (JavaScript library, not a pip package) is loaded via CDN
# in the frontend. No Python package needed.

# Core
owlready2==0.50
pandas>=2.3.3
Flask==3.1.3

# HTTP (OLS search, BioPortal/MedPortal API)
requests==2.34.2

# LLM providers (install only the ones you need)
# ──────────────────────────────────────────────
# DeepSeek / ChatGLM (OpenAI-compatible API)
openai==2.41.1
# Google Gemini (new SDK — recommended)
google-genai==2.10.0
# Google Gemini (legacy SDK — deprecated but kept as fallback)
google-generativeai==0.8.6
# Anthropic Claude
anthropic==0.109.1
